# Python
__pycache__/
.pytest_cache/
.grimp_cache/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.venv/
.mypy_cache/

# uv
.python-version.tmp

# Node
node_modules/
.pnpm-store/

# SvelteKit
frontend/build/
frontend/.svelte-kit/

# Generated API types — types-only, regenerated by codegen, so not committed.
# Unlike model-meta.ts, which is also generated but committed because it exports
# runtime values.
frontend/src/lib/api/schema.d.ts

# Generated OpenAPI schema file
backend/openapi.json

# Environment
.env
.env.local
.env.*.local

# SQLite
*.sqlite3
*.sqlite3-journal
*.sqlite3-wal
*.sqlite3-shm
db.sqlite3

# IDE
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json
.idea/
*.swp
*.swo
*~

# Claude Code
.claude/worktrees/

# OS
.DS_Store
Thumbs.db

# Django
backend/staticfiles/
backend/media/
backend/cache/

# Ingest sources (pulled from R2, not committed)
data/ingest_sources/

# DuckDB databases: the baked analytics snapshot, the production-log build
# (production_logs/analyze/), the scratch builds for plan-doc analysis (see
# docs/plans/**/*.sql), their write-ahead logs, and the tmp pairs a build
# stranded when something killed it before the rename.
*.duckdb
*.duckdb.wal
*.duckdb.tmp.*

# Production logs
production_logs/dumps/

# Scratch dumps handed to AI sessions (GSC exports, one-off data pulls).
# Anchored to the repo root on purpose: a bare `dumps/` would also match
# production_logs/analyze/fixtures/dumps/, whose test fixtures are tracked,
# and new fixtures added there would be silently ignored.
/dumps/
