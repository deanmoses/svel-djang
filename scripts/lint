#!/usr/bin/env sh
set -e
cd "$(dirname "$0")/.."

echo "==> Backend lint..."
(cd backend && uv run ruff check . && uv run ruff format --check .)

echo "==> Backend type check..."
./scripts/mypy

echo "==> Frontend lint..."
(cd frontend && pnpm lint)
