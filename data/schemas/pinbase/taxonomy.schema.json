{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "pinbase/taxonomy.schema.json",
  "title": "Pinbase Taxonomy",
  "description": "Frontmatter for a Pinbase-authored taxonomy record (cabinet, credit_role, display_type, game_format, gameplay_feature, tag)",
  "type": "object",
  "properties": {
    "slug": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" },
    "name": { "type": "string", "minLength": 1 },
    "display_order": { "type": "integer", "minimum": 0 }
  },
  "required": ["slug", "name", "display_order"],
  "additionalProperties": false
}
